# control-z — everything the Suite needs to run all six tools.
#     python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# The core algorithm tests need NONE of this (stdlib only):
#     python3 -m unittest discover -s tests -t .

# --- media pipeline (decode / detect / render) — every tool ---
numpy
av
opencv-python

# --- Slate: type rendering (lower thirds, slates, countdowns) ---
pillow

# --- the app shell (python -m suite) ---
fastapi
uvicorn[standard]
pywebview

# --- ML runtime: Rise, Depth, Scribe's diarization ---
onnxruntime

# --- Clear: dialogue rescue DSP ---
scipy
soundfile
pyloudnorm

# --- Scribe: transcription + speakers ---
faster-whisper
sherpa-onnx

# --- local model engines (Interpreter's translator, Narrator's eyes) ---
# czcore/mt_local.py and czcore/vision.py import these DIRECTLY; until now
# they arrived only as faster-whisper's transitive deps, which held exactly
# as long as faster-whisper never re-scoped its own requirements. Named
# dependencies don't vanish when a neighbor cleans house.
ctranslate2
tokenizers

# --- Community Memory: the town's own paper (PDF ingestion) ---
# pure-python, no native deps — extracts + chunks agendas, minutes and packets.
# Floor, not bare: a signed artifact should not inherit whatever PyPI resolves
# on build day from an unbounded range (the shipped version is recorded in the
# release notes).
pypdf>=6.14

# Stencil's runtime is an OPTIONAL heavy (~1 GB) — the app runs fine without
# it and its page says exactly this. It stays out of this file on purpose:
# torch is a gigabyte the other nine tools never load, and the signed app
# can't ship it. To enable click-to-matte (NOT `pip install sam2` — the name
# on PyPI is an unaffiliated fork; Meta's package comes from their repo):
#     .venv/bin/pip install -e ".[stencil]"
# or: .venv/bin/pip install torch "sam-2 @ git+https://github.com/facebookresearch/sam2.git"
# (`sam-2` is Meta's own metadata name; the import stays `sam2`)
#
# Clear's voice isolation needs the official DeepFilterNet binary (MIT/Apache).
# Everything else in Clear works without it; the app prints the download line.
