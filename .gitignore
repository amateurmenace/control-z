__pycache__/
*.pyc
.venv/
venv/
dist/
build/
*.egg-info/
.DS_Store
out/
.pytest_cache/

# editor/agent scratch
.claude/

# vendored native builds (LGPL ffmpeg prefix + sources) — rebuilt by
# packaging/build_ffmpeg.sh, never committed
packaging/vendor/
packaging/dist/
packaging/build/

# downloaded weights never live in the repo (see the Suite's Models page)
models/

# stray media: renders land beside the user's footage, not in here
*.mov
*.mp4
*.mxf
*.wav

# NOTE: no *.setting rule — depth/templates/*.setting is shipped package data.
out_*.ppm
