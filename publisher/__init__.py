"""Community Publisher — program in → clips, copy, and posts out.

The publish kit (specs/13): the moments Highlighter's scorer finds, cut in
three aspect ratios with burned captions and the station's lower-third;
titles, descriptions, alt text, a newsletter blurb and social drafts —
extractive always, generative when the user's own key is configured; a
bundle folder + zip with nothing left to rename. ~80% orchestration of
engines the suite already has, which is the point.
"""

__version__ = "0.1.0"
